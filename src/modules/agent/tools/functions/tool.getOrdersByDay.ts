// import { Tool } from './Tool.class'

/* export const toolGetOrdersByDay = new Tool({
  name: 'obtener_ordenes_por_dia',
  toolDescription: {
    type: 'function',
    function: {
      name: 'obtener_ordenes_por_dia',
      description: 'Obtiene las órdenes de producción para un día específico. La fecha debe estar en formato AAAA-MM-DD.',
    },
  },
  onEjectTool: async function (args: string) {
    const { date } = JSON.parse(args) as { date: string }
    //const orders = await getOrdersByDay(date)
    return JSON.stringify(orders, null, 2)
  },
})
 */
